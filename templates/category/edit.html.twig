{% extends 'base.html.twig' %}

{% block title %}Modifier une catégorie{% endblock %}

{% block body %}
    <div class="container  shadow mt-4">
        <div class="row">
            <div class="col-md-6 offset-md-3">
                <h1 class="h3 mb-3 font-weight-normal  p-2 rounded">
                    Modifier une catégorie
                </h1>

                {{ include('category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
<br>
                <a href="{{ path('app_category_index') }}" class="btn btn-dark">Revenir à la liste</a>

                {{ include('category/_delete_form.html.twig') }}
            </div>
        </div>
        <br>
    </div>
    <br>
{% endblock %}

