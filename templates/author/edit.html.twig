{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .form-control {
            margin-bottom: 15px;
        }
    </style>
{% endblock %}

{% block title %}Modifier Auteur{% endblock %}

{% block body %}
<div class="container shadow">
    <h1>Modifier Auteur</h1>

    {{ include('author/_form.html.twig', {'button_label': 'Modifier'}) }}
    <br>

    <a href="{{ path('app_author_index') }}" class="btn btn-dark">Retour</a>

    {{ include('author/_delete_form.html.twig') }}
    <br>
        
</div>
{% endblock %}
